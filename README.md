# Test problem for CLM using the Reuse feature

Run the test with 

```
make
```

A "swe.csv" file will be created with the CLM SWE output for the reuse values in the run.

Runs on a single node.  Problem is a single column.

